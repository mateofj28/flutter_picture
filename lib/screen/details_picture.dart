import 'package:flutter/material.dart';



class DetailsPictureScreen extends StatelessWidget {
  const DetailsPictureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container()
      ),
    );
  }
}